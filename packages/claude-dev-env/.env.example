# Copy this file to .env in this directory (same folder as this example).
# The copy is gitignored; never commit real secrets.
#
#   cp packages/claude-dev-env/.env.example packages/claude-dev-env/.env
#
# Then set your key from https://console.groq.com/keys
GROQ_API_KEY=
