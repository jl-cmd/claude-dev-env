# Archived rules

A rule lands here when it states a house preference rather than a practice an
outside authority backs. Nothing is deleted: each file keeps its history, and a
restore is one `git mv` back, plus the referrers named in its row.

| File | Why it moved | Restore |
|---|---|---|
| `claims-as-quotes.md` | Prescribes a three-piece citation shape on top of `research-mode.md`, which already requires a source. No outside authority prescribes the shape. The file declares "No hook backs it." No module, lint rule, skill, or test referenced it. | `git mv packages/claude-dev-env/rules-archived/claims-as-quotes.md packages/claude-dev-env/rules/` |
| `measurement-denominators.md` | One paragraph asking a count to name its denominator, already covered by `research-mode.md`. The file declares "No hook." No module, lint rule, skill, or test referenced it. | `git mv packages/claude-dev-env/rules-archived/measurement-denominators.md packages/claude-dev-env/rules/` |
| `hedging-claims.md` | Seven lines restating `research-mode.md`, which already requires a claim to carry its source. The file names no enforcement. | `git mv packages/claude-dev-env/rules-archived/hedging-claims.md packages/claude-dev-env/rules/` and restore the pointer line in `docs/references/prose-style-enforcement.md`. |
| `ask-user-question-required.md` | Routing a question through one tool is harness plumbing, and the caps table (2 sentences, 40 words, 15-word option descriptions) is a house measurement no style guide sets. The file declares "No hook checks this." | `git mv packages/claude-dev-env/rules-archived/ask-user-question-required.md packages/claude-dev-env/rules/` and restore its rows in `rules/code-standards.md`, `.agents/agents/clean-coder.md`, and `.agents/agents/test_agent_frontmatter.py`. |
| `confirm-implementation-forks.md` | Stopping at a material fork is defensible, but the rule makes stopping the default and buries its own exits in the last paragraph, so agents stop on work the codebase already settles. Restore it inverted, with the exits leading, rather than as written. | `git mv packages/claude-dev-env/rules-archived/confirm-implementation-forks.md packages/claude-dev-env/rules/` and restore the cross-reference in `rules/verify-before-asking.md`. |
| `prompt-workflow-context-controls.md` | House workflow scaffolding for the prompt-generator and agent-prompt pair. One commit, never edited after it landed. | `git mv packages/claude-dev-env/rules-archived/prompt-workflow-context-controls.md packages/claude-dev-env/rules/` and restore `hooks/blocking/test_context_control_policy_files.py` with its pointer in `hooks/HOOK_SPECS_PROMPT_WORKFLOW.md`. |
| `agent-spawn-protocol.md` | House guidance on how to brief a subagent. Its substance is covered by `explore-thoroughly.md` for context sufficiency and by `docs/agent-spawn-protocol.md`, which stays. | `git mv packages/claude-dev-env/rules-archived/agent-spawn-protocol.md packages/claude-dev-env/rules/` and restore its rows in `.agents/agents/clean-coder.md`, `.agents/agents/test_agent_frontmatter.py`, and `.agents/skills/orchestrator/SKILL.md`. |
| `vault-context.md` | Names one person's Obsidian vault layout, so it is preference by definition. Archiving it costs a capability rather than removing noise: it is how an agent learns to search prior decisions before starting work. Restore it if agents stop finding earlier decisions. | `git mv packages/claude-dev-env/rules-archived/vault-context.md packages/claude-dev-env/rules/` and restore its assertion in `bin/install.test.mjs`. |
| `file-global-constants.md` | "A module-level constant used by one caller belongs in that caller's scope" sets a reference-count threshold no style guide states. It was the one archived rule with a live check behind it, so `check_file_global_constants_use_count` and its tests went with the document rather than leaving code enforcing an unwritten rule. | `git mv packages/claude-dev-env/rules-archived/file-global-constants.md packages/claude-dev-env/rules/` and restore the check in `hooks/blocking/code_rules_constants_config.py`, its call in `code_rules_enforcer.py`, `hooks/blocking/test_code_rules_enforcer_file_global_constants.py`, and the rows in `clean-coder.md`, `docs/CODE_RULES.md` and `bin/install.agents-home.test.mjs`. |
