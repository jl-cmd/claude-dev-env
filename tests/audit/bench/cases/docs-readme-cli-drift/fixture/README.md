# toptokens

Count the most frequent tokens in a text file.

## Usage

```
python -m toptokens.cli FILE [--verbose] [--limit N]
```

- `--verbose` prints progress.
- `--limit N` shows the N most frequent tokens. Default 5.
