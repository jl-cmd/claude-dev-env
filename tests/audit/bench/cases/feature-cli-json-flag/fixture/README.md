# wordcount

`python -m wordcount.cli FILE` prints `lines words characters`.

Wanted: a `--json` flag that prints one JSON object with the integer keys
`lines`, `words`, and `characters` instead of the plain line.
