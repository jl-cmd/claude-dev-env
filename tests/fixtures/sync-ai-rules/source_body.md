# Code Review Rules (Test Fixture)

Review every change against these rules.

## Naming
- Require full words: `context` for `ctx`.
- Allow single-letter loop variables `i`, `j`, `k`.

## Types
- Require type hints on all parameters and return values.
