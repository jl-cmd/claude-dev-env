---
name: verify
description: Verifies claude-dev-env in a scratch home. Drives the installer, the installed hooks, and the policy linter the way a user runs them. Use to prove an installer, hook, projection, or lint change before a pull request.
---

# Verify claude-dev-env

Read `.cursor/skills/verify-claude-dev-env/SKILL.md` and follow it. It holds the launch, doctor, drive, evidence, and cleanup steps.

Pick the feature file for the changed path from `.cursor/skills/verify-claude-dev-env/features/README.md`, and drive every entry point that file lists.
