# Archived AGENTS.md files

This tree holds the full text of every repository `AGENTS.md` file.
Each live `AGENTS.md` path stays in place as a bare file.
The path under this folder matches the live path from the repository root.
Each copy is named `AGENTS.md.archive` so instruction loaders do not treat it as a live `AGENTS.md`.
